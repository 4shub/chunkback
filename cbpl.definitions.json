{
  "$schema": "./cbpl.definitions.schema.json",
  "version": "1.0.0",
  "commands": {
    "SAY": {
      "name": "SAY",
      "description": "Outputs text content as a streaming response",
      "parameters": [
        {
          "name": "content",
          "type": "string",
          "description": "The text content to output"
        }
      ],
      "examples": [
        "SAY \"Hello, World!\"",
        "SAY \"This is a test message\"",
        "SAY \"Multi-line\\ncontent\\nhere\""
      ]
    },
    "TOOLCALL": {
      "name": "TOOLCALL",
      "description": "Invokes a tool/function with specified arguments as a JSON object. Optionally specify a mocked response to complete the tool calling flow.",
      "parameters": [
        {
          "name": "toolName",
          "type": "string",
          "description": "The name of the tool to invoke"
        },
        {
          "name": "arguments",
          "type": "string",
          "description": "JSON object with arguments to pass to the tool"
        },
        {
          "name": "mockedResponse",
          "type": "string",
          "description": "Mocked response to return after the tool call"
        }
      ],
      "examples": [
        "TOOLCALL \"get_weather\" {\"location\": \"San Francisco\", \"unit\": \"celsius\"} \"The weather is 72°F and sunny\"",
        "TOOLCALL \"calculate\" {\"operation\": \"add\", \"a\": 2, \"b\": 2} \"The result is 4\"",
        "TOOLCALL \"search_web\" {\"query\": \"latest AI news\", \"limit\": 10} \"Found 10 results about AI advancements\""
      ]
    },
    "CHUNKSIZE": {
      "name": "CHUNKSIZE",
      "description": "Sets the chunk size for subsequent SAY statements",
      "parameters": [
        {
          "name": "size",
          "type": "number",
          "description": "Number of characters per chunk (1-1000)",
          "validation": {
            "min": 1,
            "max": 1000
          }
        }
      ],
      "examples": ["CHUNKSIZE 10", "CHUNKSIZE 50", "CHUNKSIZE 100"]
    },
    "CHUNKLATENCY": {
      "name": "CHUNKLATENCY",
      "description": "Sets the delay in milliseconds between chunks",
      "parameters": [
        {
          "name": "latency",
          "type": "number",
          "description": "Delay in milliseconds between chunks (0-10000)",
          "validation": {
            "min": 0,
            "max": 10000
          }
        }
      ],
      "examples": ["CHUNKLATENCY 50", "CHUNKLATENCY 100", "CHUNKLATENCY 0"]
    },
    "RANDOMLATENCY": {
      "name": "RANDOMLATENCY",
      "description": "Sets a random delay in milliseconds between chunks",
      "parameters": [
        {
          "name": "minLatency",
          "type": "number",
          "description": "Minimum delay in milliseconds between chunks (0-10000)",
          "validation": {
            "min": 0,
            "max": 10000
          }
        },
        {
          "name": "maxLatency",
          "type": "number",
          "description": "Maximum delay in milliseconds between chunks (0-10000)",
          "validation": {
            "min": 0,
            "max": 10000
          }
        }
      ],
      "examples": ["RANDOMLATENCY 50 100", "RANDOMLATENCY 100 200", "RANDOMLATENCY 0 0"]
    }
  }
}
